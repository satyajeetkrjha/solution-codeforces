#include <iostream>
#include <cstdio>
using namespace std;
typedef long long ll;
int main(){
    ll n;

    cin>>n;
      ll x=n;
    int count=0;// size
    while(n!=0){
    count++;
    n/=10;
    }
    //cout<<" count is here "<<count<<endl;
ll sum=0;
for(int i=0;i<count;i++){
    sum=(sum*10)+1;
}
//cout<<" sum is here "<<sum<<endl;
 ll ans=count*(x+1)-sum;

cout<<ans<<endl;

}
